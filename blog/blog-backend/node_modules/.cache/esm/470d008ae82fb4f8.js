let Koa,Router,bodyParser,mongoose,api,jwtMiddleware,createFakeData;_e82‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_e82‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_e82‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_e82‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_e82‍.w("./api",[["default",["api"],function(v){api=v}]]);_e82‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);_e82‍.w("./generator/createDummy",[["default",["createFakeData"],function(v){createFakeData=v}]]);require('dotenv').config();
// const Koa = require('koa');
// const Router = require('koa-router');
// const bodyParser = require('koa-bodyparser');
// const api = require('./api');
// const post = require('./post');
// const mongoose = require('mongoose');








const app = new Koa();
const router = new Router();
const { PORT, MONGO_URI } = process.env;

mongoose
  .connect(MONGO_URI, {
    useNewUrlParser: true,
    useFindAndModify: false,
    useUnifiedTopology: true,
  })
  .then(() => {
    console.log('connected to MongoDB');
    // createFakeData();
  })
  .catch((e) => {
    _e82‍.g.console.error(e);
  });

//router 설정
router.use('/api', api.routes());
//router를 app에 적용하기 전에 bodyparser 적용
app.use(bodyParser());
app.use(jwtMiddleware);
app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;

app.listen(port, () => {
  _e82‍.g.console.log('Listening to port ' + port);
});
